/**
 * Domain modules for versioned `/api/v1` routes.
 *
 * Canonical Mongoose schemas live in `@/lib/models/*` — module `*.model.ts` files re-export those.
 *
 * Domains: attendance · class · fee · leave · school · student · user
 */
